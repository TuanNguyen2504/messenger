const theme = {
  // Note: light theme
  lightTheme: {
    // Box, layout
    box: {
      bgColor: '#fff',
    },

    // Pallet color
    color: {},

    // Breakpoints
    breakpoints: {
      keys: ['xs', 'sm', 'md', 'lg', 'xl', 'xxl'],
      values: {
        xs: 480,
        sm: 576,
        md: 768,
        lg: 992,
        xl: 1200,
        xxl: 1600,
      },
    },
  },

  // Note: dark theme
  darkTheme: {
    // Box, layout
    box: {
      bgColor: '#242526',
    },

    // Pallet color
    color: {},

    // Breakpoints
    breakpoints: {
      keys: ['xs', 'sm', 'md', 'lg', 'xl', 'xxl'],
      values: {
        xs: 480,
        sm: 576,
        md: 768,
        lg: 992,
        xl: 1200,
        xxl: 1600,
      },
    },
  },
};

export default theme;
